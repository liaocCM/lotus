package handler

import "github.com/gin-gonic/gin"

// RegisterRoutes sets up the API routes.
// TODO: Implement the product CRUD handlers and register them here.
// Expected endpoints:
//   GET    /api/products       — list with pagination (?page=1&per_page=10)
//   GET    /api/products/:id   — get by ID
//   POST   /api/products       — create (validate request body)
//   PUT    /api/products/:id   — update
//   DELETE /api/products/:id   — delete
func RegisterRoutes(r *gin.Engine) {
	// TODO: implement
}
